export const environment = {
  production: true,
  // Replace with your Render backend URL after deploying
  // e.g., 'https://opauto-api.onrender.com/api'
  apiUrl: '<YOUR_RENDER_BACKEND_URL>/api',
};
